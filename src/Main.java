import java.util.HashMap;
import java.util.Map;

/**
 * Created by Paul Dennis on 4/5/2017.
 */
public class Main {

    //Map<String, isolated_package.User> userMap = new HashMap<String, isolated_package.User>();
    public static void main(String[] args) {
        //Initializaed users
        //isolated_package.User user1 = new isolated_package.User ("Paul", 26, 140);
    }

    /*class isolated_package.User {
        private String userName; //Must be unique!
        private int age;
        private int height;

        public isolated_package.User (String userName, int age, int height) {
            this.userName = userName;
            this.age = age;
            this.height = height;
        }

        public String getUserName() {
            return userName;
        }
    }*/

    /*public void addNewUser (isolated_package.User newUser) {
        userMap.put(newUser.getUserName(), newUser);
    }

    public isolated_package.User findUser (String userName) {
        return userMap.get(userName); //Note that if the userMap does not have this mapping we will be returning null!
    }*/
}
